import 'package:flutter/material.dart';

//we make constant variable to text style(i think for slider)

const kBottomContainerHeight =
    80.0; // we create the constant height which is use o height related field,

const kAllInOne =
    Color(0xFF1D1E33); // assign color value by me(name is given by me)

const kAllInOneFalse = Color(
    0xFF8D8E98); // assign the this color when first card select(means this is 2nd card)

const kBottomContainerColor = Color(
    0xFFEB1555); // assign the value for the bottom like(down of the app pink color)
////we use (k) as constant to all the variable to make easy for the suggestion in programing,

const kLabelTextStyle = TextStyle(
  //its is used for the slider option
  fontSize: 18.0,
  color: Color(0xFF8D8E98),
);

const kNumberTextStyle = TextStyle(
  //its is used for the slider option
  fontSize: 50.0, fontWeight: FontWeight.w900,
);

//we create the constant value which help to add the thing(font size,weight) in the BMI calculator.
const kLargeButtonTextStyle = TextStyle(
  fontSize: 25.0,
  fontWeight: FontWeight.bold,
);
// its used for the result Page(look and feel of the page)
const kTittleTextStyle = TextStyle(
  fontSize: 50.0,
  fontWeight: FontWeight.bold,
);
// its is used for the result page
const kResultTextStyle = TextStyle(
  color: Color(0xFF24D876),
  fontSize: 22.0,
  fontWeight: FontWeight.bold,
);

//its is used in result page
const kBMITextStyle = TextStyle(
  fontSize: 80.0,
  fontWeight: FontWeight.bold,
);

const kBodyTextStyle = TextStyle(
  fontSize: 22.0,
);
